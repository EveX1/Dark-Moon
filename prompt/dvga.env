# ==== Fichier ENV de test pour DVGA ====
# Tout ce qui est ici doit se retrouver dans :
# - Katana (-H ...)
# - Arjun  (-H ...)
# - curl/http_status
# - ZAP    (via le proxy)

# Header de test bien visible
HEADER=X-DM-Test: DVGA-ENV-OK

# Cookie bidon pour voir s'il passe partout (ne sert pas à s'authentifier)
COOKIE=PHPSESSID=deadbeefcafebabe

# UA custom pour le différencier dans les logs
USER_AGENT=Darkmoon-AgentFactory-Test/0.1

# (optionnel) Contexte ZAP si tu en utilises un
# Si tu ne mets rien, ton contexte ZAP reste géré comme avant.
#ZAP_CONTEXT=DVGA-Authenticated